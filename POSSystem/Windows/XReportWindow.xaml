<Window x:Class="POSSystem.Windows.XReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="X-Report (Shift Report)" 
        Width="750" Height="850"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F0F0F0"
        FontFamily="Segoe UI, Tahoma">

    <Window.Resources>

        <!-- Button face gradient -->
        <LinearGradientBrush x:Key="BtnFace" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#FDFDFD" Offset="0"/>
            <GradientStop Color="#F2F2F2" Offset="0.5"/>
            <GradientStop Color="#E8E8E8" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="BtnHover" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#EEF4FF" Offset="0"/>
            <GradientStop Color="#DBE9FF" Offset="0.5"/>
            <GradientStop Color="#C8DBFA" Offset="1"/>
        </LinearGradientBrush>

        <!-- Header - soft green (X-Report theme) -->
        <LinearGradientBrush x:Key="HeaderBrush" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#E8F5E9" Offset="0"/>
            <GradientStop Color="#C8E6C9" Offset="1"/>
        </LinearGradientBrush>

        <!-- Bevel brush -->
        <LinearGradientBrush x:Key="BevelNormal" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FFFFFF" Offset="0"/>
            <GradientStop Color="#FFFFFF" Offset="0.49"/>
            <GradientStop Color="#A0A0A0" Offset="0.50"/>
            <GradientStop Color="#A0A0A0" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="BevelPress" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#A0A0A0" Offset="0"/>
            <GradientStop Color="#A0A0A0" Offset="0.49"/>
            <GradientStop Color="#FFFFFF" Offset="0.50"/>
            <GradientStop Color="#FFFFFF" Offset="1"/>
        </LinearGradientBrush>

        <!-- 3D Bevel Button -->
        <Style x:Key="BigBtn" TargetType="Button">
            <Setter Property="Foreground" Value="#1A1A1A"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="Segoe UI, Tahoma"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="OuterBorder"
                                BorderBrush="#B8B8B8" BorderThickness="1"
                                CornerRadius="3">
                            <Border x:Name="InnerBorder"
                                    BorderBrush="{StaticResource BevelNormal}"
                                    BorderThickness="1" CornerRadius="2">
                                <Border x:Name="FaceBorder"
                                        Background="{StaticResource BtnFace}"
                                        CornerRadius="1"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                </Border>
                            </Border>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="FaceBorder" Property="Background" Value="{StaticResource BtnHover}"/>
                                <Setter TargetName="OuterBorder" Property="BorderBrush" Value="#4488CC"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="FaceBorder" Property="Background" Value="#D0D0D0"/>
                                <Setter TargetName="InnerBorder" Property="BorderBrush" Value="{StaticResource BevelPress}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="FaceBorder" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Refresh button - Green accent -->
        <Style x:Key="RefreshBtn" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Segoe UI, Tahoma"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="24,12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="OuterBorder"
                                BorderBrush="#2E7D32" BorderThickness="1"
                                CornerRadius="3">
                            <Border x:Name="InnerBorder"
                                    BorderBrush="{StaticResource BevelNormal}"
                                    BorderThickness="1" CornerRadius="2">
                                <Border x:Name="FaceBorder"
                                        Background="#4CAF50"
                                        CornerRadius="1"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                </Border>
                            </Border>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="FaceBorder" Property="Background" Value="#388E3C"/>
                                <Setter TargetName="OuterBorder" Property="BorderBrush" Value="#1B5E20"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="FaceBorder" Property="Background" Value="#2E7D32"/>
                                <Setter TargetName="InnerBorder" Property="BorderBrush" Value="{StaticResource BevelPress}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="#F0F0F0" Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="Auto"/>
            <!-- Info bar -->
            <RowDefinition Height="*"/>
            <!-- Report Content -->
            <RowDefinition Height="Auto"/>
            <!-- Buttons -->
        </Grid.RowDefinitions>

        <!-- ══ HEADER ══ -->
        <Border Grid.Row="0" Background="{StaticResource HeaderBrush}"
                BorderBrush="#A5D6A7" BorderThickness="0,0,0,1"
                Padding="20,16">
            <StackPanel>
                <Label Content="📋  X-REPORT (SHIFT REPORT)" 
                       FontSize="26" FontWeight="Bold"
                       Foreground="#1B5E20" Padding="0"
                       HorizontalAlignment="Center"
                       FontFamily="Segoe UI, Tahoma"/>
                <Label x:Name="lblDate" Content="Date: --"
                       FontSize="14" FontWeight="SemiBold"
                       Foreground="#555555" 
                       Padding="0,4,0,0" HorizontalAlignment="Center"
                       FontFamily="Segoe UI, Tahoma"/>
            </StackPanel>
        </Border>

        <!-- ══ INFO BANNER ══ -->
        <Border Grid.Row="1" Background="#E3F2FD" 
                BorderBrush="#90CAF9" BorderThickness="0,0,0,1"
                Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="ℹ️" FontSize="24" 
                           Margin="0,0,12,0" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" 
                           Text="This is a mid-shift report showing current sales progress. You can generate multiple X-Reports during a shift. Z-Report closes the day."
                           TextWrapping="Wrap"
                           Foreground="#1565C0" FontSize="13" FontWeight="SemiBold"
                           FontFamily="Segoe UI, Tahoma"
                           VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- ══ REPORT CONTENT ══ -->
        <Border Grid.Row="2" Background="#FAFAFA" 
                BorderBrush="#C0C0C0" BorderThickness="0,1"
                Margin="20,16">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          Padding="24">
                <StackPanel x:Name="spXReport">
                    <!-- Content generated in code -->
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- ══ BUTTONS ══ -->
        <Border Grid.Row="3" Background="#F5F5F5"
                BorderBrush="#D0D0D0" BorderThickness="0,1,0,0"
                Padding="20,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" x:Name="btnPrint" 
                        Content="🖨️  Print Report" 
                        Style="{StaticResource BigBtn}" 
                        Margin="0,0,8,0"
                        HorizontalAlignment="Left"
                        Click="btnPrint_Click"/>
                <Button Grid.Column="1" x:Name="btnSavePdf"
                        Content="📄  Save PDF" 
                        Style="{StaticResource BigBtn}" 
                        Margin="0,0,8,0"
                        Click="btnSavePdf_Click"/>
                <Button Grid.Column="2" x:Name="btnRefresh"
                        Content="🔄  Refresh" 
                        Style="{StaticResource RefreshBtn}" 
                        Margin="0,0,12,0"
                        Click="btnRefresh_Click"/>
                <Button Grid.Column="3" x:Name="btnClose"
                        Content="✕  Close" 
                        Style="{StaticResource BigBtn}"
                        IsCancel="True" 
                        Click="btnClose_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>